---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "nomad_variable Ephemeral Resource - terraform-provider-nomad"
subcategory: ""
description: |-
  Reads a Nomad variable when its items are needed by Terraform but should not be stored in state.
---

# nomad_variable (Ephemeral Resource)

Reads a Nomad variable when its items are needed by Terraform but should not be stored in state.

## Example Usage

```terraform
resource "nomad_variable" "example" {
  path = "some/path/of/your/choosing"

  items_wo = jsonencode({
    example_key = "example_value"
  })

  items_wo_version = 1
}

ephemeral "nomad_variable" "example" {
  path = nomad_variable.example.path
}

resource "some_resource" "example" {
  secret_value_wo         = ephemeral.nomad_variable.example.items.example_key
  secret_value_wo_version = 1
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `path` (String) The path of the variable.

### Optional

- `namespace` (String) Variable namespace. Defaults to default.

### Read-Only

- `items` (Map of String, Sensitive) A map of values from the stored variable.
